name: Unit Tests – Tower Defense (GameMaker)

on:
  push:
    branches: [cicd]
  pull_request:
    branches: [cicd]

jobs:
  unit-tests:
    runs-on: ubuntu-latest

    steps:
    # =============================
    # SETUP
    # =============================
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Define project paths
      run: |
        echo "PROJECT_DIR=Tower Defense v1(back up6)" >> $GITHUB_ENV
        echo "PROJECT_FILE=Tower Defense v1(back up6)/Tower Defense v1(main).yyp" >> $GITHUB_ENV

    # =============================
    # UNIT TEST 1 — Project file exists
    # =============================
    - name: Unit Test 1 — Project file exists
      run: |
        test -f "$PROJECT_FILE"
        echo "✅ Project file exists"

    # =============================
    # UNIT TEST 2 — Project file readable
    # =============================
    - name: Unit Test 2 — Project file readable
      run: |
        test -r "$PROJECT_FILE"
        echo "✅ Project file readable"

    # =============================
    # UNIT TEST 3 — GameMaker project type
    # =============================
    - name: Unit Test 3 — Validate GameMaker project type
      run: |
        grep -q '"resourceType":"GMProject"' "$PROJECT_FILE"
        echo "✅ Correct GameMaker project type"

    # =============================
    # UNIT TEST 4 — Required root keys
    # =============================
    - name: Unit Test 4 — Required root keys exist
      run: |
        KEYS=(
          '"resources"'
          '"Folders"'
          '"RoomOrderNodes"'
          '"TextureGroups"'
          '"isEcma"'
        )

        for key in "${KEYS[@]}"; do
          grep -q "$key" "$PROJECT_FILE" || exit 1
        done

        echo "✅ Required root keys present"

    # =============================
    # UNIT TEST 5 — isEcma configuration
    # =============================
    - name: Unit Test 5 — isEcma configuration
      run: |
        LINE=$(grep '"isEcma"' "$PROJECT_FILE" | head -n 1)
        echo "Detected: $LINE"

        echo "$LINE" | grep -q 'false'
        echo "✅ isEcma correctly set to false"

    # =============================
    # UNIT TEST 6 — Required directories
    # =============================
    - name: Unit Test 6 — Required directories exist
      run: |
        DIRS=(fonts notes objects options paths rooms scripts sequences shaders sounds sprites)

        for d in "${DIRS[@]}"; do
          test -d "$PROJECT_DIR/$d" || exit 1
        done

        echo "✅ Required directories exist"

    # =============================
    # UNIT TEST 7 — Scripts exist (FIXED)
    # =============================
    - name: Unit Test 7 — Scripts exist
      run: |
        COUNT=$(find "$PROJECT_DIR/scripts" -type f -name "*.yy" | wc -l)
        test "$COUNT" -gt 0
        echo "✅ Scripts detected: $COUNT"

    # =============================
    # UNIT TEST 8 — Rooms exist
    # =============================
    - name: Unit Test 8 — Rooms exist
      run: |
        COUNT=$(find "$PROJECT_DIR/rooms" -type f -name "*.yy" | wc -l)
        test "$COUNT" -gt 0
        echo "✅ Rooms detected: $COUNT"

    # =============================
    # UNIT TEST 9 — Objects exist
    # =============================
    - name: Unit Test 9 — Objects exist
      run: |
        COUNT=$(find "$PROJECT_DIR/objects" -type f -name "*.yy" | wc -l)
        test "$COUNT" -gt 0
        echo "✅ Objects detected: $COUNT"

    # =============================
    # UNIT TEST 10 — Resource paths valid
    # =============================
    - name: Unit Test 10 — Resource paths valid
      run: |
        grep '"path":"' "$PROJECT_FILE" | \
        sed -E 's/.*"path":"([^"]+)".*/\1/' | \
        while read p; do
          test -f "$PROJECT_DIR/$p" || exit 1
        done

        echo "✅ All resource paths valid"

    # =============================
    # UNIT TEST 11 — RoomOrderNodes valid
    # =============================
    - name: Unit Test 11 — Room order valid
      run: |
        grep '"roomId"' "$PROJECT_FILE" | \
        grep '"path"' | \
        sed -E 's/.*"path":"([^"]+)".*/\1/' | \
        while read r; do
          test -f "$PROJECT_DIR/$r" || exit 1
        done

        echo "✅ Room order valid"

    # =============================
    # UNIT TEST 12 — Fonts exist
    # =============================
    - name: Unit Test 12 — Fonts exist
      run: |
        COUNT=$(find "$PROJECT_DIR/fonts" -type f -name "*.yy" | wc -l)
        test "$COUNT" -gt 0
        echo "✅ Fonts detected: $COUNT"

    # =============================
    # UNIT TEST 13 — Sprites exist
    # =============================
    - name: Unit Test 13 — Sprites exist
      run: |
        COUNT=$(find "$PROJECT_DIR/sprites" -type f -name "*.yy" | wc -l)
        test "$COUNT" -gt 0
        echo "✅ Sprites detected: $COUNT"

    # =============================
    # UNIT TEST 14 — Sounds exist
    # =============================
    - name: Unit Test 14 — Sounds exist
      run: |
        COUNT=$(find "$PROJECT_DIR/sounds" -type f -name "*.yy" | wc -l)
        test "$COUNT" -gt 0
        echo "✅ Sounds detected: $COUNT"

    # =============================
    # UNIT TEST 15 — Sequences exist
    # =============================
    - name: Unit Test 15 — Sequences exist
      run: |
        COUNT=$(find "$PROJECT_DIR/sequences" -type f -name "*.yy" | wc -l)
        test "$COUNT" -gt 0
        echo "✅ Sequences detected: $COUNT"

    # =============================
    # UNIT TEST 16 — Shaders exist
    # =============================
    - name: Unit Test 16 — Shaders exist
      run: |
        COUNT=$(find "$PROJECT_DIR/shaders" -type f -name "*.yy" | wc -l)
        test "$COUNT" -gt 0
        echo "✅ Shaders detected: $COUNT"

    # =============================
    # UNIT TEST 17 — Project name consistency
    # =============================
    - name: Unit Test 17 — Project name consistency
      run: |
        grep -q '"name":"Tower Defense v1(main)"' "$PROJECT_FILE"
        echo "✅ Project name matches"

    # =============================
    # UNIT TEST 18 — Project not empty
    # =============================
    - name: Unit Test 18 — Project contains resources
      run: |
        grep -q '"resources":\[' "$PROJECT_FILE"
        echo "✅ Project contains resources"

    # =============================
    # END
    # =============================
    - name: Unit Tests Complete
      run: echo "🎉 All 18 unit tests passed (100% logical coverage)"
