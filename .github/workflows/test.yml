name: Unit Tests – Tower Defense (GameMaker)

on:
  push:
    branches: [cicd]
  pull_request:
    branches: [cicd]

jobs:
  unit-tests:
    runs-on: ubuntu-latest

    steps:
    # ==================================================
    # SETUP
    # ==================================================
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Define project paths
      run: |
        echo "PROJECT_DIR=Tower Defense v1(back up6)" >> $GITHUB_ENV
        echo "PROJECT_FILE=Tower Defense v1(back up6)/Tower Defense v1(main).yyp" >> $GITHUB_ENV

    # ==================================================
    # UNIT TEST 1 — Project file exists
    # ==================================================
    - name: Unit Test 1 — Project file exists
      run: |
        test -f "$PROJECT_FILE"
        echo "✅ Project file exists"

    # ==================================================
    # UNIT TEST 2 — Project file readable
    # ==================================================
    - name: Unit Test 2 — Project file readable
      run: |
        test -r "$PROJECT_FILE"
        echo "✅ Project file readable"

    # ==================================================
    # UNIT TEST 3 — GameMaker project type
    # ==================================================
    - name: Unit Test 3 — Validate GameMaker project type
      run: |
        grep -q '"resourceType":"GMProject"' "$PROJECT_FILE"
        echo "✅ Correct GameMaker project type"

    # ==================================================
    # UNIT TEST 4 — Required root keys
    # ==================================================
    - name: Unit Test 4 — Required root keys exist
      run: |
        KEYS=(resources Folders RoomOrderNodes TextureGroups isEcma)
        for k in "${KEYS[@]}"; do
          grep -q "\"$k\"" "$PROJECT_FILE" || exit 1
        done
        echo "✅ Required root keys present"

    # ==================================================
    # UNIT TEST 5 — isEcma configuration
    # ==================================================
    - name: Unit Test 5 — isEcma configuration
      run: |
        grep '"isEcma"' "$PROJECT_FILE" | grep -q 'false'
        echo "✅ isEcma correctly set to false"

    # ==================================================
    # UNIT TEST 6 — Required directories
    # ==================================================
    - name: Unit Test 6 — Required directories exist
      run: |
        DIRS=(fonts notes objects options paths rooms scripts sequences shaders sounds sprites)
        for d in "${DIRS[@]}"; do
          test -d "$PROJECT_DIR/$d" || exit 1
        done
        echo "✅ Required directories exist"

    # ==================================================
    # UNIT TEST 7 — Scripts exist
    # ==================================================
    - name: Unit Test 7 — Scripts exist
      run: |
        find "$PROJECT_DIR/scripts" -type f -name "*.yy" | grep -q .
        echo "✅ Scripts detected"

    # ==================================================
    # UNIT TEST 8 — Objects exist
    # ==================================================
    - name: Unit Test 8 — Objects exist
      run: |
        find "$PROJECT_DIR/objects" -type f -name "*.yy" | grep -q .
        echo "✅ Objects detected"

    # ==================================================
    # UNIT TEST 9 — Rooms exist
    # ==================================================
    - name: Unit Test 9 — Rooms exist
      run: |
        find "$PROJECT_DIR/rooms" -type f -name "*.yy" | grep -q .
        echo "✅ Rooms detected"

    # ==================================================
    # UNIT TEST 10 — Resource paths valid
    # ==================================================
    - name: Unit Test 10 — Resource paths valid
      run: |
        grep '"path":"' "$PROJECT_FILE" | \
        sed -E 's/.*"path":"([^"]+)".*/\1/' | \
        while read p; do
          test -f "$PROJECT_DIR/$p" || exit 1
        done
        echo "✅ All resource paths valid"

    # ==================================================
    # UNIT TEST 11 — No duplicate resource paths
    # ==================================================
    - name: Unit Test 11 — No duplicate resource paths
      run: |
        DUPES=$(grep '"path":"' "$PROJECT_FILE" | sort | uniq -d)
        test -z "$DUPES"
        echo "✅ No duplicate resource paths"

    # ==================================================
    # UNIT TEST 12 — RoomOrderNodes valid
    # ==================================================
    - name: Unit Test 12 — RoomOrderNodes valid
      run: |
        grep '"roomId"' "$PROJECT_FILE" | grep '"path"' | \
        sed -E 's/.*"path":"([^"]+)".*/\1/' | \
        while read r; do
          test -f "$PROJECT_DIR/$r" || exit 1
        done
        echo "✅ Room order references valid rooms"

    # ==================================================
    # UNIT TEST 13 — Fonts exist
    # ==================================================
    - name: Unit Test 13 — Fonts exist
      run: |
        find "$PROJECT_DIR/fonts" -type f -name "*.yy" | grep -q .
        echo "✅ Fonts detected"

    # ==================================================
    # UNIT TEST 14 — Sprites exist
    # ==================================================
    - name: Unit Test 14 — Sprites exist
      run: |
        find "$PROJECT_DIR/sprites" -type f -name "*.yy" | grep -q .
        echo "✅ Sprites detected"

    # ==================================================
    # UNIT TEST 15 — Sounds exist
    # ==================================================
    - name: Unit Test 15 — Sounds exist
      run: |
        find "$PROJECT_DIR/sounds" -type f -name "*.yy" | grep -q .
        echo "✅ Sounds detected"

    # ==================================================
    # UNIT TEST 16 — Sequences exist
    # ==================================================
    - name: Unit Test 16 — Sequences exist
      run: |
        find "$PROJECT_DIR/sequences" -type f -name "*.yy" | grep -q .
        echo "✅ Sequences detected"

    # ==================================================
    # UNIT TEST 17 — Shaders exist
    # ==================================================
    - name: Unit Test 17 — Shaders exist
      run: |
        find "$PROJECT_DIR/shaders" -type f -name "*.yy" | grep -q .
        echo "✅ Shaders detected"

    # ==================================================
    # UNIT TEST 18 — Resources array not empty
    # ==================================================
    - name: Unit Test 18 — Project contains resources
      run: |
        grep -q '"resources":\[' "$PROJECT_FILE"
        echo "✅ Resources array exists"

    # ==================================================
    # UNIT TEST 19 — AudioGroups exist
    # ==================================================
    - name: Unit Test 19 — AudioGroups exist
      run: |
        grep -q '"AudioGroups"' "$PROJECT_FILE"
        echo "✅ AudioGroups present"

    # ==================================================
    # UNIT TEST 20 — TextureGroups include Default
    # ==================================================
    - name: Unit Test 20 — TextureGroups include Default
      run: |
        grep -q '"TextureGroups"' "$PROJECT_FILE"
        grep -q '"Default"' "$PROJECT_FILE"
        echo "✅ Default TextureGroup present"

    # ==================================================
    # UNIT TEST 21 — Folders list exists
    # ==================================================
    - name: Unit Test 21 — Folders list exists
      run: |
        grep -q '"Folders":\[' "$PROJECT_FILE"
        echo "✅ Folders list present"

    # ==================================================
    # UNIT TEST 22 — RoomOrderNodes list exists
    # ==================================================
    - name: Unit Test 22 — RoomOrderNodes list exists
      run: |
        grep -q '"RoomOrderNodes":\[' "$PROJECT_FILE"
        echo "✅ RoomOrderNodes list present"

    # ==================================================
    # UNIT TEST 23 — RoomOrderNodes referenced in resources
    # ==================================================
    - name: Unit Test 23 — RoomOrderNodes referenced in resources
      run: |
        grep '"roomId"' "$PROJECT_FILE" | grep '"path"' | \
        sed -E 's/.*"path":"([^"]+)".*/\1/' | \
        while read r; do
          grep -q "$r" "$PROJECT_FILE" || exit 1
        done
        echo "✅ RoomOrderNodes paths referenced in resources"

    # ==================================================
    # UNIT TEST 24 — Project template type
    # ==================================================
    - name: Unit Test 24 — Project templateType
      run: |
        grep -q '"templateType":"game"' "$PROJECT_FILE"
        echo "✅ Correct project template type"

    # ==================================================
    # END
    # ==================================================
    - name: Unit Tests Complete
      run: echo "🎉 All 24 unit tests passed — polished validation complete"
