name: CI – Tower Defense

on:
  push:
    branches: [cicd]
  pull_request:
    branches: [cicd]

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: CI Banner
        run: |
          echo "🚀 CI Pipeline Started"
          echo "Branch: cicd"
          echo "Commit: $GITHUB_SHA"

      - name: Validate GameMaker project exists
        run: |
          ls "Tower Defense v1(back up6)"/*.yyp > /dev/null
          echo "✅ GameMaker project detected"

      - name: CI Finished
        run: echo "🎉 CI completed successfully"
