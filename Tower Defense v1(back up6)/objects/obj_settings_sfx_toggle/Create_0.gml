// Define a collision mask
mask_index = -1; // This allows custom collision
sprite_index = -1; // No sprite, we're drawing manually

// Make sure object has dimensions
width = 200;  // Already there
height = 40;  // Already there

// Enable mouse events
image_xscale = 1;
image_yscale = 1;
