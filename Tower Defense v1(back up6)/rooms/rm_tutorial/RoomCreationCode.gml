// In obj_tutorial Create Event, add this:
display_set_gui_size(1920, 1080); // Or your game resolution
// obj_back_button - Create Event
  // Lower number = drawn on top

// obj_tutorial - Create Event

var _stopper = instance_create_depth(0, 0, 0, obj_music_stopper);

instance_create_depth(0, 0, 0, obj_bg_settings);